```mermaid
sequenceDiagram
    participant browser
    participant server

    Note left of browser: Submit note, then POST req
    browser->>server: HTTP POST https://studies.cs.helsinki.fi/exampleapp/notes
    activate server
    Note right of server: Add new note to array,
    return 302, reload page
    server-->>browser: HTTP 302 - URL redirect
    deactivate server

```